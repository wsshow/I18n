before:
  hooks:
    - go mod tidy
release:
  skip_upload: true
  github:
    owner: wsshow
    name: I18n
snapshot:
  name_template: '{{ .Tag }}-next'
changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
